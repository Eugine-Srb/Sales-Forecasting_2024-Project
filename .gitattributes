# Normalize line endings for all text files
* text=auto

# Force LF for YAML files (e.g. environment.yml)
*.yml text eol=lf

# If you add large Power BI binaries, track them with LFS:
# *.pbix filter=lfs diff=lfs merge=lfs -text
